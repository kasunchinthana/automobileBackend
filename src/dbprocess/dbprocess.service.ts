import { Injectable } from '@nestjs/common';
import { InjectQueue } from '@nestjs/bull';
import { Queue } from 'bull';

@Injectable()
export class DbprocessService {
    // constructor(@InjectQueue('dbcsv') private dbcsvQueue: Queue) {
  
    // }
    // async registerQueue(){
    //    // const job = await this.dbcsvQueue.add('dbqueue',data);
    // }
      
    
      
}
